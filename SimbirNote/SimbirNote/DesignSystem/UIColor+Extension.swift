//
//  UIColor+Extension.swift
//  SimbirNote
//
//  Created by Mac on 16.12.2024.
//

import UIKit

extension UIColor {
    /// Помидор
    static let dsRed = UIColor(hex: "#d32d3a") ?? .white
    /// Мандарин
    static let dsOrange = UIColor(hex: "#ea5635") ?? .white
    /// Банан
    static let dsYellow = UIColor(hex: "#e7b63a") ?? .white
    /// Базилик
    static let dsGreen = UIColor(hex: "#348054") ?? .white
    /// Шалфей
    static let dsMint = UIColor(hex: "#3ca86e") ?? .white
    /// Павлин
    static let dsTeal = UIColor(hex: "#2898db") ?? .white
    /// Черника
    static let dsBlue = UIColor(hex: "#4d5aaf") ?? .white
    /// Лаванда
    static let dsIndigo = UIColor(hex: "#6b74bf") ?? .white
    /// Виноград
    static let dsPurple = UIColor(hex: "#8c38a5") ?? .white
    /// Фламинго
    static let dsPink = UIColor(hex: "#d8706a") ?? .white
    /// Графитос
    static let dsGray = UIColor(hex: "#606060") ?? .white
    /// Цвет по умолчанию
    static let dsWhite = UIColor(hex: "#ffffff") ?? .white
}
